#!/usr/bin/python3
def uniq_add(my_list=[]):
    unique_set = set()
    total = 0
    for num in my_list:
        for num not in unique_list:
            total += num
        unique_list.append(num)
    return total
